import ScopeContainer from 'components/ScopeManage/ScopeContainer';
import MainLayout from 'layouts/MainLayout';

const Scope = () => (
  <MainLayout isFixedDatatable={true}>
    <ScopeContainer />
  </MainLayout>
);

export default Scope;
